df['New_col'] = df.apply(lambda row: row['due'] if row['due'] and not row['target'] else row['target'] if row['target'] else ('2/2/2222' if pd.isnull(row['due']) and pd.isnull(row['target']) else row['target']), axis=1)
